enact: "2.0.0"
name: enact/hello-rust
version: "1.0.1"
description: A simple Rust greeting tool
from: rust:1.83-slim

hooks:
  build:
    - rustc /work/hello.rs -o /work/hello

scripts:
  greet: "/work/hello {{name}}"
